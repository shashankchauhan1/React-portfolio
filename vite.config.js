import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/React-portfolio/', // Add this line to specify the subdirectory for GitHub Pages
})
